method: bayes
name: counterfactual_overfitting
metric:
  goal: maximize
  name: validation/accuracy
parameters:
  alpha:
    min: 0.001
    max: 0.9
  radius:
    min: 0.001
    max: 1.5
  n_samples:
    min: 50
    max: 500
  