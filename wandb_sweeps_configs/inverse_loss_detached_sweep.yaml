method: grid
name: Inverse_Loss_Detached_Norm_Sweep
metric:
  goal: maximize
  name: validation/accuracy
parameters:
  loss.alpha:
    values: [0.01, 0.05, 0.1, 0.2, 0.5, 1.0]
  loss.type:
    values: ["scfe_inverse_regularization"]
  estimator.type:
    values: ["latent_scfe_detached"]
  model.model_type:
    values: ["BMLP"]
  seed:
    values: [42]
